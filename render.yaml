services:
  - type: web
    name: bidso-labs-public
    runtime: static
    branch: main
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_BASE
        value: https://bidso-labs-internal.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
